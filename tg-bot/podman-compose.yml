name: arbiter

services:
  tg-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: arbiter-tg-bot
    env_file:
      - .env
    environment:
      DB_PATH: /data/arbiter.db
    volumes:
      - arbiter-data:/data
    restart: unless-stopped

volumes:
  arbiter-data:
